import React from 'react';

// Styles
import { Wrapper, Content } from './Header.styles';

const Header = () => (
    <Wrapper>
        <Content>
            <h1>Take note!</h1>
        </Content>
    </Wrapper>
);

export default Header;